# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Overview

This is a **GitHub profile README repository** (`harrylowkey/harrylowkey`). It renders as the profile page at github.com/harrylowkey. The repo contains only:

- `README.md` — the profile content (HTML + Markdown with GitHub stats widgets and social links)
- `assets/github-snake.svg` — animated contribution snake graphic

## Key Details

- No build system, package manager, or tests — this is a static Markdown/HTML repo
- The README uses GitHub-flavored Markdown with embedded HTML (`<p>`, `<a>`, `<img>` tags)
- External widget images are pulled from `streak-stats.demolab.com` and `github-readme-stats.vercel.app`
- The snake SVG in `assets/` is typically generated by a GitHub Action (e.g., `platane/snk`)
- Personal site: harrylowkey.dev
